# 本地后台额度与用量统计

Windows 任务计划名称：`Codex-Local-Usage-Hourly`。每小时启动一次 Python，采样完成后退出，无常驻模型对话。登录 Windows 后可在 Codex 界面关闭时运行；注销或关机时不运行，睡眠期间错过的采样在系统恢复可运行时补一次，不伪造过去的额度快照。

## 数据与费用

- 使用 `codex app-server` 的 `account/rateLimits/read` 查询账号额度，需要联网和有效的 Codex 登录态。
- 不调用 `codex exec`、`thread/start` 或 `turn/start`，不发送提示词，不产生模型推理 Token。
- 本机 Token 统计复用 `collect-local-usage.py`，继续增量读取 `.codex` 日志，沿用原有历史和去重缓存。
- 额度获取失败仍统计本机用量，并记录错误；不会用旧额度填充新记录。
- 独立 app-server 若未返回 accountId，则历史中该字段为空，不沿用旧账号 ID；此时无法可靠判断账号切换。
- 本机日志 Token 与账号剩余额度属于不同统计口径。

## 文件

- `local-monitor.py`：单次采样入口。
- `manage-local-monitor.ps1`：安装、查看、停用、启用和移除计划任务。
- `local-monitor-status.json`：最近一次运行状态。
- `local-monitor-history.jsonl`：每次运行简报（含错误）。
- `quota-history.jsonl` / `local-usage-history.jsonl`：继续使用的额度和本机用量历史。
- `quota-errors.jsonl`：额度查询错误。

## 管理

在本目录 PowerShell 执行：

```powershell
./manage-local-monitor.ps1 -Action Status
./manage-local-monitor.ps1 -Action Stop
./manage-local-monitor.ps1 -Action Start
./manage-local-monitor.ps1 -Action Remove
```

重新安装或更换 `.codex` 路径：

```powershell
./manage-local-monitor.ps1 -Action Install -CodexRoot 'D:\my-codex' -PythonPath 'C:\Python313\pythonw.exe'
```

Python 需 3.9+，仅标准库，无需 pip 安装。Windows 任务使用当前用户、普通权限，不保存密码。推荐 pythonw.exe 避免弹出窗口。Codex 可执行文件从 PATH 或用户安装目录自动寻找，单次运行也可传 `--codex-exe` 指定。

官方接口：https://learn.chatgpt.com/docs/app-server


## 首次安装与查看面板

1. 安装并登录 Codex，确认能使用 `codex app-server`。本后台程序仅支持 Windows；面板原有跨平台功能不受影响。
2. 在仓库目录运行 `./manage-local-monitor.ps1 -Action Install`。默认使用 Codex 附带的 Python；找不到时按上文传入 Python 的实际路径。安装约一分钟后首次运行，随后每小时运行。
3. 自定义数据目录时显式传入 `-CodexRoot`，并将面板 `config.json` 的 `codex_root` 设置为同一路径。后台程序不会自动读取面板配置。
4. 在面板 `config.json` 设置 `"quota_dir": "."`，然后停止并重新打开面板。保留其他配置字段。
5. 查看 `local-monitor-status.json`，正常应为 `status: ok`。退出码 1 表示警告或失败，查看其中的 `quota_error`、`local_error`、`local_warnings`。错误只写本地，不会发送 Codex 通知。

首次采样建立本机用量基线，不回填过去的新增 Token。额度为实际采样时的快照，不能补造过去的快照。面板的历史对话用量仍从日志独立读取。

手动采样（不安装任务）：

```powershell
python ./local-monitor.py --codex-root 'D:\my-codex'
```

## 从旧目录迁移

本次加入仓库只包含源码，不自动迁移或重装已运行的计划任务。现有任务仍可继续写入旧目录，面板的 `quota_dir` 也可继续指向旧目录。

要改为从仓库运行：先在旧目录停用任务并确认没有正在采样的进程；将旧目录的 `quota-history.jsonl`、`local-usage-history.jsonl`、`local-usage-state.sqlite` 一起复制到新目录（不要覆盖新目录已有数据），再从新目录执行 Install。同名任务会更新为新路径。最后修改面板的 `quota_dir`。不要同时运行两份采样器指向同一份历史文件。

## 排错与分享

- 执行策略阻止脚本时，可对单次命令使用 `powershell -NoProfile -ExecutionPolicy Bypass -File .\manage-local-monitor.ps1 -Action Install`，不修改全局策略。
- 公司策略拒绝注册任务时，请联系 IT；无需保存 Windows 密码。
- 查询超时或登录失效时，本机用量仍采集；重新登录 Codex 后等待下一次采样。
- 仓库忽略采样历史、状态、数据库和锁文件；分享源码不包含这些个人运行数据。
