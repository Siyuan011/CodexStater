# Codex 本机动态用量面板

## 日常使用

双击本目录的 **`打开用量面板.cmd`**。它会在后台启动本地服务，并打开浏览器；服务已运行时会复用，不会重复启动。

首次运行会自动从 `config.example.json` 创建本机 `config.json`，查找 Python 3.9+ 和 `.codex` 数据目录，无需手动复制配置或修改用户名。未找到数据目录时会弹出文件夹选择窗口；选中 `.codex` 本身后会保存位置，供下次使用。启动器不会扫描整块硬盘，也不会自动安装 Python。

默认地址：**http://127.0.0.1:8766**。服务运行期间，也可以直接用浏览器书签打开这个地址。

- **打开即更新**：每次打开页面自动读取新增日志。
- **持续更新**：页面可见时默认每 60 秒刷新；页面隐藏时暂停轮询，切回时刷新。
- **时间范围**：可切换过去 24 小时、过去 7 天，或点击旁边的“自定义时间范围”，选择开始和结束日期、时间后应用。预设是截至更新时间的滚动窗口；自定义范围保持固定，刷新不会移动边界。输入使用面板显示的时区（默认北京时间），精确到分钟；包含开始时刻，不包含结束时刻。单次最多 366 天，结束时间不能晚于当前时间。汇总、图表、排行、推理强度和额度历史一起切换。
- **固定纵轴**：小时图每格为 2.5M Token，10M 对应四格，最少显示四格。超过 10M 时按固定格高继续向上延伸；例如 20M 对应八格，柱子不会因用量更多而按比例缩小。
- **每小时明细**：鼠标放到柱形上，显示该小时总 Token、前 8 个对话及各自用量/占比，其余对话汇总为“其他”；点击柱形查看全部对话。
- **键盘/触屏**：也可使用图表下方的“查看小时”下拉框与“展开明细”按钮。
- **筛选**：按模型、推理强度、排除本统计任务、排除内部审批筛选；点击对话排行榜中的名字可只看该对话。筛选同时作用于汇总、小时图、明细、模型与强度对照表和排行。
- **推理强度**：例如先选 `gpt-6-astra`，强度保留“全部”可对比 Ultra、中等档的用量，选择具体强度可只看该档。对话中途换档会分别归属；日志没记录强度时显示“未记录”，不推断为默认档位。
- **对话排行**：默认仅显示用量最高的 5 条，可点击“展开其余”或“收起”。收起仅改变展示条数，不改变汇总、图表、占比或导出数据；切换筛选后恢复收起。
- **用量占比图**：排行下方的“模型与推理强度用量”提供环形图和明细表，各扇区按当前筛选的模型与强度组合分组，面积表示总 Token 占比；空数据会显示暂无用量。
- **立即刷新**：手动读取新增日志。尚未落盘的模型回复需等待后续刷新才能计入。

关闭浏览器不会停止服务，但没有页面访问时不会持续扫描日志。需要停止时，双击 **`停止服务.cmd`**。下次打开面板会重新启动。电脑重启后需要再次打开启动入口；本工具没有创建开机启动项或系统定时任务。

## 导出最新 HTML

有两种方式：

1. 面板右上角 **“导出 HTML”**：下载当前数据和筛选状态，保留 24 小时、7 天及已查询的自定义范围；离线也能切换这些已保存的范围、筛选和查看明细。选择新的自定义范围需要回到本机动态面板重新查询，再导出。
2. 双击 **`导出最新HTML.cmd`**：先读取最新数据，再在本目录生成并打开 `最新用量报告.html`，默认显示 24 小时，可切换 7 天。

导出的 HTML 是快照。想每次都看到最新数据，请打开启动入口或本地网址，而不是反复打开旧的导出文件。

## 配置 .codex 路径与其他参数

Windows 一键入口按以下顺序选择数据目录：命令行 `-CodexRoot`、配置中有效的 `codex_root`、`CODEX_HOME` 环境变量、当前用户主目录下的 `.codex`。配置里的旧路径已不存在时，会提示并自动尝试后续位置；均未找到时，正常打开面板或导出会弹出文件夹选择窗口。启动器会按配置文件记住上次成功启动时的数据目录，停止服务时复用该位置，因此目录已移动或环境变量已改变也不会阻止停止。

通常无需修改配置。自定义位置可通过文件夹选择窗口、`CODEX_HOME` 或 `config.json` 指定。修改配置前先停止服务，保存修改后再重新打开面板。

```json
{
  "codex_root": "D:/Profiles/.codex",
  "quota_dir": "D:/Codex用量统计",
  "cache_dir": ".cache",
  "port": 8766,
  "refresh_seconds": 60,
  "timezone": "+08:00",
  "monitor_thread_id": ""
}
```

| 字段 | 说明 |
| --- | --- |
| `codex_root` | `.codex` 本身的路径。通用配置默认留空，使用 `CODEX_HOME` 或当前用户主目录下的 `.codex`。Windows 一键入口会跳过已经失效的配置路径。支持中文、空格、`~` 与相对路径。 |
| `quota_dir` | 保存原每小时巡检 `quota-history.jsonl` 的目录。默认留空，只显示本机 Token；需要账号额度图时，填写已有历史记录的目录。 |
| `cache_dir` | 本工具的缓存目录，默认本目录下 `.cache`；不向原 `.codex` 写文件。 |
| `port` | 本机端口，默认 8766。遇到占用时可改成其他 1024–65535 的端口。 |
| `refresh_seconds` | 页面刷新间隔，默认 60 秒，最小 5 秒。通常无需缩短。 |
| `timezone` | 固定时区偏移，默认 `+08:00`（北京时间）；不会自动处理夏令时。 |
| `monitor_thread_id` | 可选，用于识别本统计任务并支持排除。留空则隐藏对应选项。 |

JSON 路径建议使用正斜杠 `/`；若使用反斜杠，需写成 `\\`。相对路径以配置文件所在目录为基准。

`config.example.json` 是不含个人路径的通用示例。Windows 一键入口会在配置缺失时自动复制；macOS/Linux 或直接使用 Python 命令时，请手动复制为 `config.json`。分享项目时保留示例，不附带本机 `config.json`。

## 命令行用法

需要 **Python 3.9+**，全部使用标准库，无需安装第三方 Python 包。Windows 一键脚本依次检查 `STATER_PYTHON` 指定的解释器、`py`、`python`、`python3` 和 Codex 已有的 Python，并验证是否能够运行及满足版本要求。没有可用 Python 时会显示安装提示。

三个 `.cmd` 共用 `launch.ps1`，可接收这些参数：

```powershell
# 只检查解释器、配置和数据目录，不启动服务或打开浏览器
.\打开用量面板.cmd -Check

# 启动但不打开浏览器
.\打开用量面板.cmd -NoBrowser

# 指定自定义数据目录或配置文件
.\打开用量面板.cmd -CodexRoot "D:\Profiles\.codex"
.\打开用量面板.cmd -Config "D:\Settings\stater.json"

# 自动化运行时，不弹出文件夹选择窗口；找不到数据目录则报错
.\导出最新HTML.cmd -NonInteractive -NoBrowser
```

`-Check` 仅检查路径，不启动或导出；`-NoBrowser` 也适用于导出，不会自动打开生成的 HTML。使用 `-Config` 启动后，停止服务时也应指定同一配置文件。

也可以在本目录直接调用 Python：

```powershell
# 后台启动并打开浏览器
py -3 .\app.py launch

# 后台启动，但不打开浏览器
py -3 .\app.py launch --no-browser

# 指定数据目录和端口
py -3 .\app.py launch --codex-root "D:\Profiles\.codex" --quota-dir "D:\Codex用量统计" --port 8767

# 使用指定配置文件
py -3 .\app.py launch --config .\config.json

# 导出最新静态 HTML
py -3 .\app.py export --output .\最新用量报告.html

# 停止服务
py -3 .\app.py stop

# 前台启动，便于排查问题；Ctrl+C 停止
py -3 .\app.py serve
```

用参数覆盖配置启动时，停止命令也应带相同参数；更推荐将长期设置保存到 `config.json`。macOS/Linux 可以将 `py -3` 替换为 `python3`；Windows 的 `.cmd` 一键入口仅用于 Windows。

## 统计方式与性能

- 默认首次读取近期日志，建立约 8 天的索引缓冲。首次使用自定义范围时，会补读本机现存的历史日志，包括旧缓存曾跳过的记录；日志较多时需要稍等。此后保留已索引的历史用量，只增量读取变化，关闭或重启服务后可以继续使用。缓存保存在 `.cache/usage-*.sqlite`，不修改原始日志；已丢失且未缓存的历史无法恢复。
- 24 小时、7 天和自定义范围均按事件的精确时间边界查询，不按比例估算小时用量。因为窗口可能从半点开始，24 小时和 7 天通常会显示 25 或 169 个小时片段；自定义范围也会保留首尾不足一小时的片段。
- 总 Token = 输入 + 输出。缓存输入包含在输入中，推理输出包含在输出中，不重复相加。悬停与完整明细会按对话合并不同模型的记录，分项合计与柱形一致。
- 推理强度取自每轮日志设置，并按事件发生时的设置归属。`Ultra`、`Max` 等档位分别保留；“推理 Token”是输出中的实际用量，不等于强度设置。不同强度的历史用量反映各自实际任务，不能仅凭总量判断回答质量或得出固定消耗倍数。
- 升级后会自动补读旧缓存对应的日志，以回填推理强度，不修改原始日志。无法从日志恢复的强度保留为“未记录”；后续继续增量读取，无需手动清空缓存。
- 导出 HTML 保留强度数据；页面按钮导出还保留当前强度筛选。旧快照没有强度字段的记录按“未记录”处理。
- 对相同时间、相同用量信息的复制事件去重。累计计数重置时使用该次请求计数；复制/分叉历史优先归属创建更早的原始会话。复杂迁移或不完整日志仍可能造成归属不完整。
- 任务名取自本机任务显示名称；不把原始提示词或对话正文当作标题。无法读取名称时显示会话 ID。
- 切换账号、其他机器、云端和 PID 归属无法仅由本机日志可靠还原。这里的 Token 统计不是官方费用账单。
- 直接日志按事件时间计数，原每小时巡检按发现新增日志的时间计数，两者可能因基线、时间边界和延迟落盘而不同。
- 账号剩余额度只读取现有巡检文件，仍是每小时更新；页面标注账号采样时间。额度窗口变化或账号变化处断开曲线，不把回升视为负消耗。
- 面板刷新、索引与静态导出均不调用模型或 API，不消耗模型 Token。原 Codex 每小时巡检仍独立运行，可能有其自身的少量 Token 开销。

## 文件结构

```text
_stater/
  app.py                  本地服务、后台启动/停止、静态导出
  usage_store.py          增量日志索引、精确时间汇总、额度历史读取
  launch.ps1              Windows Python、数据目录检测与共用启动入口
  打开用量面板.cmd
  停止服务.cmd
  导出最新HTML.cmd
  config.json             当前机器配置
  config.example.json     分享用示例配置
  使用说明.md
  web/
    index.html            页面结构
    dashboard.css         页面样式
    logic.js              共用聚合、筛选与明细计算
    dashboard.js          刷新、绘图、悬停、导出等交互
  tests/
    test_store.py          日志、去重、增量、时间窗口验证
    test_logic.cjs         对话汇总、Top 8/其他合计、筛选验证
  .cache/                 运行时生成，不应分享
  最新用量报告.html        导出的个人数据快照
```

## 排错与分享

- **首次加载较慢**：首次需要读取最近活跃的大日志；第一次查询自定义范围还需要补读历史日志，请稍等。后续只解析变化内容。
- **页面刷新失败**：旧数据会保留并显示错误，不会把旧数据标成新数据。检查 `.cache/server.log`。
- **启动入口失败**：先运行 `打开用量面板.cmd -Check` 查看检测结果，按错误提示检查 Python 与配置路径；也可查看 `.cache/launcher-error.txt`。
- **换电脑后找不到数据**：启动器会跳过旧配置中已失效的路径，查找 `CODEX_HOME` 和当前用户的 `.codex`；自定义位置可在文件夹选择窗口中选取。不要把其他电脑的个人配置一起复制。
- **端口占用/配置不一致**：先用旧配置停止服务，再修改配置重启；或选择另一个端口。不需要结束其他应用的进程。
- **要重建索引**：先停止服务，将 `.cache` 改名备份，再重新启动；首次索引会重新生成。不要删除原 `.codex` 日志。
- **分享源码**：复制代码、`web`、`tests`、说明和示例配置即可；不分享 `.cache`、当前 `config.json` 或含个人数据的 HTML。网页只监听 `127.0.0.1`，不向局域网或公网提供服务，不读取 `auth.json`。

运行测试（开发者可选）：

```powershell
py -3 -m unittest discover -s tests -v
node tests/test_logic.cjs
```

Node.js 仅用于最后一项开发测试；正常运行不需要 Node.js。


## 本地后台采样

可安装 Python 后台程序，每小时读取账号额度并记录本机新增 Token，无需 Codex 模型巡检。见 [本地后台巡检说明](本地后台巡检说明.md)。
